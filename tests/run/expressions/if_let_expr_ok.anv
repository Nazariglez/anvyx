// @mode: run
// @expect: success
// @match-begin
// 42
// 0
// @match-end

fn main() {
    let x: int? = 42;
    let y = if let Option.Some(v) = x { v } else { 0 };
    println(y);

    let z: int? = nil;
    let w = if let Option.Some(v) = z { v } else { 0 };
    println(w);
}
