// @mode: run
// @expect: success
// @contains: ok

fn twice(x: int) -> int {
    x * 2
}

fn quadruple(x: int) -> int {
    twice(twice(x))
}

fn main() {
    let result = quadruple(3);
    assert(result == 12);
    println("ok");
}
