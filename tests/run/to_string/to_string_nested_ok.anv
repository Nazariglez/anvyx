// @mode: run
// @expect: success
// @match: Wrapper(inner: custom!, x: 42)

struct Inner {
    val: int,

    fn to_string(self) -> string {
        "custom!"
    }
}

struct Wrapper { inner: Inner, x: int }

fn main() {
    println(Wrapper { inner: Inner { val: 99 }, x: 42 });
}
