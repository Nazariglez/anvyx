// @mode: run
// @expect: success
// @match: rgb(1.0, 0.0, 0.0)

struct Color {
    r: float, g: float, b: float,

    fn to_string(self) -> string {
        "rgb(" + self.r + ", " + self.g + ", " + self.b + ")"
    },
}

fn main() {
    println(Color { r: 1.0, g: 0.0, b: 0.0 });
}
