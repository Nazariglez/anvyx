// @mode: run
// @expect: success
// @match: [11, 12, 13]

fn add(var x: int, amount: int) {
    x = x + amount;
}

fn main() {
    var xs: [int] = [1, 2, 3];
    xs.for_each(|var x| add(x, 10));
    println(xs);
}
