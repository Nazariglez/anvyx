// @mode: run
// @expect: success
// @match: 10

fn twice(x: int) -> int {
    return x * 2;
}

fn apply(f: fn(int) -> int, x: int) -> int {
    return f(x);
}

fn main() {
    println(apply(twice, 5));
}
