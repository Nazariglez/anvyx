// @mode: run
// @expect: success
// @match: done

fn noop() {}

dataref Node {
    callback: fn() -> void,
}

fn main() {
    var a = Node { callback: noop };
    var b = Node { callback: noop };
    a.callback = || { b.callback(); };
    b.callback = noop;
    println("done");
}
