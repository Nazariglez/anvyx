// @mode: run
// @expect: success
// @match: [2, 20, 4]

fn early_exit(var x: int) {
    if x > 10 {
        return;
    }
    x += 1;
}

fn main() {
    var xs: [int] = [1, 20, 3];
    xs.for_each(early_exit);
    println(xs);
}
