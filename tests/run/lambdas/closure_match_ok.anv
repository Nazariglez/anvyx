// @mode: run
// @expect: success
// @match-begin
// 6
// 5
// @match-end

fn pick(x: int) -> fn(int) -> int {
    return match x {
        1 => |y: int| y * 2,
        _ => |y: int| y + 2,
    };
}

fn main() {
    let f = pick(1);
    println(f(3));
    let g = pick(0);
    println(g(3));
}
