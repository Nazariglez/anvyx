// @mode: run
// @expect: success
// @contains: ok

import std.maps { * };

fn main() {
    var m = ordered<int, string>();
    m[1] = "one";
    m[2] = "two";
    let v: string? = m[1];
    assert(v != nil);
    println("ok");
}
