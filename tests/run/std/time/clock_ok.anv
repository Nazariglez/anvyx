// @mode: run
// @expect: success
// @match-begin
// now=true
// since=true
// @match-end

import std.time;

fn main() {
    let ms = time.now();
    println(f"now={ms > 1_000_000_000_000}");

    let start = time.elapsed();
    let dt = time.since(start);
    println(f"since={dt >= 0.0d}");
}
