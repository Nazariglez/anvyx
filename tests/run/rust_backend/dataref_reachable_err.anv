// @mode: run
// @expect: error
// @contains: Rust backend: unsupported dataref
// @skip: rust-only test — run with: cargo run -p test-runner -- tests/run/rust_backend --backend rust

dataref Node {
    value: int,
    label: string,
}

fn main() {
    let n = Node { value: 42, label: "hello" };
}
