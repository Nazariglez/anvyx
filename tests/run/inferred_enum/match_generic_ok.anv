// @mode: run
// @expect: success
// @match-begin
// val: 42
// empty
// @match-end

enum Wrapper<T> { Val(T), Empty }

fn show(w: Wrapper<int>) {
    match w {
        .Val(v) => println("val: {v}"),
        .Empty => println("empty"),
    }
}

fn main() {
    show(.Val(42));
    show(.Empty);
}
