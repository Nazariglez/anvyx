// @mode: run
// @expect: success
// @match: 99

struct Vec2 { x: int, y: int }
struct Entity { pos: Vec2 }

extend Entity {
    fn set_x(var self, val: int) {
        self.pos.x = val;
    }
}

fn main() {
    var e = Entity { pos: Vec2 { x: 0, y: 0 } };
    e.set_x(99);
    println(e.pos.x);
}
