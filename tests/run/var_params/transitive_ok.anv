// @mode: run
// @expect: success
// @match: 12

fn inner(var x: int) {
    x += 1;
}

fn outer(var x: int) {
    inner(x);
}

fn main() {
    var n = 10;
    outer(n);
    outer(n);
    println(n);
}
