// @mode: run
// @expect: success
// @contains: ok

fn main() {
    let t = true;
    let f = false;
    assert(t && t);
    assert(!(t && f));
    assert(t || f);
    assert(!(f || f));
    assert(!f);
    assert(!(!t));
    println("ok");
}
