// @mode: run
// @expect: success
// @contains: ok

fn main() {
    let a = -42;
    let b = -1;
    let c = -0;
    assert(a == -42);
    assert(b == -1);
    assert(c == 0);
    let x = 5;
    let neg_x = -x;
    assert(neg_x == -5);
    println("ok");
}
