// @mode: run
// @expect: success
// @contains: ok

fn main() {
    let a = 1.5;
    let b = 2.5;
    let sum = a + b;
    let diff = b - a;
    let prod = a * b;
    assert(sum == 4.0);
    assert(diff == 1.0);
    assert(prod == 3.75);
    println("ok");
}
