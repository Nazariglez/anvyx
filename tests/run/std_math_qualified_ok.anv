// @mode: run
// @expect: success
// @contains: ok

import std.math;

fn main() {
    assert(math.sqrt(9.0) == 3.0);
    assert(math.floor(2.7) == 2.0);
    assert(math.ceil(2.3) == 3.0);
    assert(math.round(2.5) == 3.0);
    println("ok");
}
