// @mode: run
// @expect: success
// @match: 1.0 2.0

struct Vec2 { x: float, y: float }

extend Vec2 {
    cast from(v: float) { Vec2 { x: v, y: v } }
}

fn move_by(offset: as Vec2) -> Vec2 { offset }

fn main() {
    let v = move_by(Vec2 { x: 1.0, y: 2.0 });
    println(f"{v.x} {v.y}");
}
