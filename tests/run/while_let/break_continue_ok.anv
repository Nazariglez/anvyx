// @mode: run
// @expect: success
// @match-begin
// 20
// 40
// @match-end

enum Step {
    Next(int),
    Done,
}

fn get_step(i: int) -> Step {
    if i < 5 {
        Step.Next((i + 1) * 10)
    } else {
        Step.Done
    }
}

fn main() {
    var i = 0;
    while let Step.Next(val) = get_step(i) {
        i = i + 1;
        if val % 20 != 0 { continue; }
        if val > 40 { break; }
        println(val);
    }
}
