// @mode: run
// @expect: success
// @match: 15

fn main() {
    var opt: int? = 10;
    if let Option.Some(var n) = opt {
        n += 5;
    }
    if let Option.Some(v) = opt {
        println(v);
    }
}
