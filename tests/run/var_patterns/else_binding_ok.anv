// @mode: run
// @expect: success
// @match: 42

fn main() {
    var opt: int? = nil;
    match opt {
        Option.Some(n) => {},
        var other => { other = 42; },
    }
    if let Option.Some(v) = opt {
        println(v);
    }
}
