// @mode: run
// @expect: success

fn main() {
    let m = [1: "one", 2: "two"];
    let v1: string? = m[1];
    let v2: string? = m[2];
    let v3: string? = m[3];
    assert(v1 != nil);
    assert(v2 != nil);
    assert(v3 == nil);
}
