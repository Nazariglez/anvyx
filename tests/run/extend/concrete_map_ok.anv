// @mode: run
// @expect: success
// @match: true

extend [string: int] {
    fn has_key(self, k: string) -> bool {
        self[k] != nil
    }
}

fn main() {
    let m = ["a": 1, "b": 2];
    println(m.has_key("a"));
}
