// @mode: run
// @expect: success
// @match: 6

extend [int; 3] {
    fn sum(self) -> int {
        var total = 0;
        for v in self {
            total += v;
        }
        total
    }
}

fn main() {
    let a: [int; 3] = [1, 2, 3];
    println(a.sum());
}
