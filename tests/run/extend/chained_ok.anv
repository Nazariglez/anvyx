// @mode: run
// @expect: success
// @match: 12.0

extend float {
    fn double_val(self) -> float { self * 2.0 }
    fn triple_val(self) -> float { self * 3.0 }
}

fn main() {
    println(2.0.double_val().triple_val());
}
