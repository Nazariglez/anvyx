// @mode: run
// @expect: success
// @match-begin
// int-specific
// generic
// @match-end

import generic;
import specific;

fn main() {
    let nums: [int] = [1, 2, 3];
    let strs: [string] = ["a", "b"];
    println(nums.label());
    println(strs.label());
}
