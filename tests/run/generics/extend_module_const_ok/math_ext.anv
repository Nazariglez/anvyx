// @helper

const SCALE = 100;

pub extend<T> [T] {
    fn scaled_len(self) -> int {
        var count = 0;
        for _ in self {
            count = count + 1;
        }
        count * SCALE
    }
}
