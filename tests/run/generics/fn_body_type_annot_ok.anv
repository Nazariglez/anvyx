// @mode: run
// @expect: success
// @match: [1, 2, 3]

fn to_list<T>(a: [T; 3]) -> [T] {
    var result: [T] = [];
    for item in a {
        result.push(item);
    }
    result
}

fn main() {
    let xs: [int; 3] = [1, 2, 3];
    println(to_list<int>(xs));
}
