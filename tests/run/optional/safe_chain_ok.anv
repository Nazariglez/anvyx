// @mode: run
// @expect: success
// @skip: safe chain wraps result in Option.Some() (findings.md #6)
// @match-begin
// 5
// nil
// @match-end

struct Inner { value: int }
struct Outer { inner: Inner }

fn main() {
    let a: Outer? = Outer { inner: Inner { value: 5 } };
    println(a?.inner?.value);

    let b: Outer? = nil;
    println(b?.inner?.value);
}
