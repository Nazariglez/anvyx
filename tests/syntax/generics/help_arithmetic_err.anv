// @mode: check
// @expect: error
// @contains: operator '-' cannot be applied to 'string'
// @contains: must be int, float, or double

fn sub<T>(a: T, b: T) -> T {
    return a - b;
}

fn main() {
    let x = sub("hello", "world");
}
