// @mode: check
// @expect: error
// @contains: not an array or list
// @contains: must be an array, list, or slice

fn get_first<T>(a: T) -> int {
    return a[0];
}

fn main() {
    let x = get_first(42);
}
