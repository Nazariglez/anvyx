// @mode: check
// @expect: success

extend float {
    /// Returns true if this float is close to `other`.
    fn near(self, other: float) -> bool {
        self - other < 0.0001
    }
}

fn main() {}
