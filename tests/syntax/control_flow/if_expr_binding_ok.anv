// @mode: check
// @expect: success

fn main() {
    let a: bool = if true { false } else { true };
    let b = if a { a } else { !a };
    let cond: bool = true;
    let c: int = cond ? 1 : 2;
    let d = cond ? c : c * 2;
}

