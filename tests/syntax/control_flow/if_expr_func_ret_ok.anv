// @mode: check
// @expect: success

fn scale_if(a: int, nice: bool) -> int {
    if nice { a } else { a * 10 }
}

fn scale_ternary(a: int, nice: bool) -> int {
    nice ? a : a * 10
}

fn main() {
    let x: int = scale_if(2, true);
    let y: int = scale_ternary(2, true);
}

