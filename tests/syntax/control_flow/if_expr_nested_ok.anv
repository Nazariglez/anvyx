// @mode: check
// @expect: success

fn sign(x: int) -> int {
    if x < 0 { -1 }
    else if x == 0 { 0 }
    else { 1 }
}

fn classify(x: int) -> int {
    x < 0 ? -1
    : x == 0 ? 0
    : 1
}

fn main() {
    let s: int = sign(10);
    let t: int = classify(10);
}

