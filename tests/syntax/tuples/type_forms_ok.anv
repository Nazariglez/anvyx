// @mode: check
// @expect: success

fn main() {
    let p: (int, int) = (1, 2);
    let t: (int, string, bool) = (1, "hi", true);
    let nested: ((int, int), string) = ((1, 2), "ok");
    let maybe_pair: (int, int)? = (10, 20);
    let maybe_par_2: (int, string)? = nil;

    let point: (x: int, y: int) = (x: 10, y: 20);
    let rect: (w: int, h: int) = (w: 100, h: 200);
}
