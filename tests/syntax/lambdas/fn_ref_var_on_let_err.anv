// @mode: check
// @expect: error
// @contains: requires the target to be declared with 'var'

fn mutate(var x: int) {
    x *= 2;
}

fn main() {
    let xs: [int] = [1, 2, 3];
    xs.for_each(mutate);
}
