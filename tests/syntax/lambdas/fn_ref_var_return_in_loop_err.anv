// @mode: run
// @expect: error
// @contains: contains 'return' inside a loop

fn bad_fn(var x: int) {
    while x < 100 {
        if x > 10 {
            return;
        }
        x += 1;
    }
}

fn main() {
    var xs: [int] = [1, 20, 3];
    xs.for_each(bad_fn);
}
