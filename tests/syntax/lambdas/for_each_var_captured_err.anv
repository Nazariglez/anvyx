// @mode: check
// @expect: error
// @contains: cannot mutate captured variable

fn main() {
    var xs: [int] = [1, 2, 3];
    var count = 0;
    xs.for_each(|var x| {
        x += 1;
        count += 1;
    });
}
