// @mode: check
// @expect: success

fn apply(f: fn(int) -> int, x: int) -> int {
    return f(x);
}

fn nested(f: fn(fn(int) -> int) -> int, g: fn(int) -> int) -> int {
    return f(g);
}

fn main() {}
