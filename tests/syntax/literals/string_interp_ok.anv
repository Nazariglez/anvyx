// @mode: check
// @expect: success

fn main() {
    let hp = 100;
    let name: string = "Link";
    let x = 1.5;
    let alive = true;

    let a: string = f"HP: {hp}";
    let b: string = f"{name} has {hp} HP";
    let c: string = f"pos: {x}";
    let d: string = f"alive: {alive}";
    let e: string = f"{hp + 1}";
    let f: string = f"escaped \{brace}";
}
