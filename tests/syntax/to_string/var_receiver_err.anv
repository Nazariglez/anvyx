// @mode: check
// @expect: error
// @contains: must be 'self', not 'var self'

struct Foo {
    x: int,

    fn to_string(var self) -> string {
        "foo"
    },
}

fn main() {
    let f = Foo { x: 1 };
}
