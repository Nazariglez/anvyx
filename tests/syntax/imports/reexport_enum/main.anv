// @mode: check
// @expect: success

import facade { Color };

fn main() {
    let c = Color.Red;
    let is_red = match c {
        Color.Red => true,
        Color.Green => false,
        Color.Blue => false,
    };
    let r: bool = is_red;
}
