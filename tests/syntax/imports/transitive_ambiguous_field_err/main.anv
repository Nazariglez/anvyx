// @mode: check
// @expect: error
// @contains: ambiguous
// @skip: this is not ambigous, alpha.Item is what comes from get_item, no one has imported beta.Item and it doesn't matter if we did because the signature give us the type

import facade { get_item };
import beta;

fn main() {
    let item = get_item();
    println(item.value);
}
