// @mode: check
// @expect: error
// @contains: Invalid operand type
// @contains: cannot be applied to 'string'

dataref Pair<T> {
    a: T,
    b: T,

    fn diff(self) -> T {
        return self.a - self.b;
    }
}

fn main() {
    let p = Pair { a: "x", b: "y" };
    let d = p.diff();
}
