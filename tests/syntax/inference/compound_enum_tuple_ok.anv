// @mode: check
// @expect: success

enum Wrap<T> {
    Items([T]),
    None,
}

fn main() {
    let nums: [int] = [1, 2, 3];
    let w = Wrap.Items(nums);

    let strs: [string] = ["a", "b"];
    let v = Wrap.Items(strs);
}
