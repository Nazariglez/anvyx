// @mode: check
// @expect: success

fn first<T>(items: [T]) -> T {
    items[0]
}

fn main() {
    let ints: [int] = [1, 2, 3];
    let x = first(ints);
    let _: int = x;

    let strs: [string] = ["a", "b"];
    let y = first(strs);
    let _: string = y;
}
