// @mode: check
// @expect: success

enum Event {
    Move { dx: int, dy: int },
    Hit { target: int, damage: int },
}

fn main() {
    let e1 = Event.Move { dx: 5, dy: 10 };
    let e2 = Event.Hit { target: 1, damage: 25 };
}
