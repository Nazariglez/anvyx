// @mode: check
// @expect: success

enum Color {
    Red,
    Green,
    Blue,
}

fn main() {
    let c1 = Color.Red;
    let c2 = Color.Red;
    let same = c1 == c2;
    let c3 = Color.Blue;
    let different = c1 != c3;
}
