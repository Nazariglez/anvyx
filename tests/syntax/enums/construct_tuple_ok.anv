// @mode: check
// @expect: success

enum Message {
    Ping(int),
    Move(int, int),
    Data(string),
}

fn main() {
    let m1 = Message.Ping(42);
    let m2 = Message.Move(10, 20);
    let m3 = Message.Data("hello");
}
