// @mode: check
// @expect: error
// @contains: mutating method

struct Counter { value: int }

extend Counter {
    fn inc(var self) {
        self.value = self.value + 1;
    }
}

fn main() {
    let opt: Counter? = Counter { value: 0 };
    opt?.inc();
}
