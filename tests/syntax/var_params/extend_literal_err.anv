// @mode: check
// @expect: error
// @contains: non-lvalue

struct Num { value: int }

extend Num {
    fn set_value(var self, var v: int) {
        self.value = v;
    }
}

fn main() {
    var n = Num { value: 0 };
    n.set_value(5);
}
