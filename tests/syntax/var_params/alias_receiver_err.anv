// @mode: check
// @expect: error
// @contains: same variable

struct Counter {
    value: int,

    fn add(var self, var amount: int) {
        self.value = self.value + amount;
    }
}

fn main() {
    var c = Counter { value: 10 };
    c.add(c.value);
}
