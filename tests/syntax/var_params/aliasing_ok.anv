// @mode: check
// @expect: success

fn swap(var a: int, var b: int) {
    let tmp = a;
    a = b;
    b = tmp;
}

fn add_to(var target: int, amount: int) {
    target = target + amount;
}

fn main() {
    var x = 10;
    var y = 20;
    swap(x, y);
    add_to(x, x);
}
