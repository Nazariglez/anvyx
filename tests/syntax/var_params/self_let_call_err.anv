// @mode: check
// @expect: error
// @contains: mutating method

struct Counter {
    value: int,

    fn increment(var self) {
        self.value = self.value + 1;
    },
}

fn main() {
    let c = Counter { value: 0 };
    c.increment();
}
