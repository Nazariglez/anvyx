// @mode: check
// @expect: error
// @contains: Invalid operand type

struct Foo {
    fn bad<T>(self, x: T) -> T {
        x - x
    }
}

fn main() {
    let f = Foo {};
    let a: string = f.bad("oops");
}
