// @mode: check
// @expect: success

struct Foo {
    fn bar<T>(self, x: T) -> T {
        x
    },
}

fn main() {
    let f = Foo {};
    let a: int = f.bar<int>(42);
    let b: string = f.bar<string>("hello");
}
