// @mode: check
// @expect: error
// @contains: readonly method

struct Counter {
    value: int,

    fn bump(self) -> void {
        self.value += 1;
    }
}

fn main() {
    let c = Counter { value: 0 };
    c.bump();
}
