// @mode: check
// @expect: success

struct Wrapper<T> {
    value: T,
}

fn main() {
    let a = Wrapper { value: 42 };
    let b = Wrapper { value: "hi" };
    let x: int = a.value;
    let y: string = b.value;

    let c: Wrapper<int> = Wrapper { value: 42 };
    let z: int = c.value;
}
