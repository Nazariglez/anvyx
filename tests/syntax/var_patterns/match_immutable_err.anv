// @mode: check
// @expect: error
// @contains: mutable

enum Color { Red, Green, Blue }

fn main() {
    let c = Color.Red;
    match c {
        Color.Red => { println("red"); },
        var other => { println("other"); },
    }
}
