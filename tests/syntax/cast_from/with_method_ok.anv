// @mode: check
// @expect: success

struct Vec2 { x: float, y: float }

extend Vec2 {
    fn length(self) -> float { 0.0 }
    cast from(v: float) { Vec2 { x: v, y: v } }
}

fn main() {
    let v = Vec2 { x: 1.0, y: 2.0 };
    let l = v.length();
}
