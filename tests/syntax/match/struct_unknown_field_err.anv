// @mode: check
// @expect: error
// @contains: Unknown field

enum Event { Move { dx: int } }

fn main() {
    let e = Event.Move { dx: 1 };
    match e {
        Event.Move { dx, dy } => {},
    }
}
