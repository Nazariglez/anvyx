// @mode: check
// @expect: success

enum GameState { Menu, Playing }

fn main() {
    let state = GameState.Menu;
    let name: string = match state {
        GameState.Menu => "Menu",
        GameState.Playing => "Playing",
    };
}
