// @mode: check
// @expect: error
// @contains: Missing field

enum Event { Move { dx: int, dy: int } }

fn main() {
    let e = Event.Move { dx: 1, dy: 2 };
    match e {
        Event.Move { dx } => {},
    }
}
