// @mode: check
// @expect: success

enum GameState { Menu, Playing, Paused }

fn main() {
    let state = GameState.Playing;
    match state {
        GameState.Menu => {},
        GameState.Playing => {},
        GameState.Paused => {},
    }
}
