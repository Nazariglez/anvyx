// @mode: check
// @expect: success
// @warn-contains: accessing internal field 'cache' of type 'Item'

import lib { Item };

fn main() {
    let i = Item { name: "test", cache: "" };
    println(i.cache);
}
