// @mode: check
// @expect: error
// @contains: Invalid operand type

extern type Vec2 {
    x: float;
    y: float;
    init;
    op Self + Self -> Self;
}

fn main() {
    let a = Vec2 { x: 1.0, y: 2.0 };
    let b = Vec2 { x: 3.0, y: 4.0 };
    let c = a < b;
}
