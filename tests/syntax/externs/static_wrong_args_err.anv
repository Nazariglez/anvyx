// @mode: check
// @expect: error
// @contains: Mismatched types

extern type Point {
    x: float;
    y: float;
    fn new(x: float, y: float) -> Self;
}

fn main() {
    let p = Point.new("bad", 2.0);
}
