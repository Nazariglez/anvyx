// @mode: check
// @expect: success

extern type Point {
    x: float;
    y: float;
}

extern fn make_point(x: float, y: float) -> Point;

fn main() {
    var p = make_point(1.0, 2.0);
    p.x += 3.0;
    p.y -= 1.0;
    p.x *= 2.0;
    p.y /= 0.5;
}
