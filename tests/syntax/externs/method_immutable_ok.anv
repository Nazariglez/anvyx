// @mode: check
// @expect: success

extern type Rect {
    w: float;
    h: float;
    fn area(self) -> float;
}

extern fn make_rect(w: float, h: float) -> Rect;

fn main() {
    let r = make_rect(10.0, 20.0);
    let a: float = r.area();
}
