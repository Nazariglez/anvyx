// @mode: check
// @expect: error
// @contains: Mismatched types

extern type Vec2 {
    x: float;
    y: float;
    init;
    op Self + Self -> Self;
}

fn main() {
    let a = Vec2 { x: 1.0, y: 2.0 };
    let b = a + 2.0;
}
