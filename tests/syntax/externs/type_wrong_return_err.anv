// @mode: check
// @expect: error
// @contains: Mismatched types

extern type Sprite;
extern type Sound;
extern fn create_sound(path: string) -> Sound;

fn make_sprite() -> Sprite {
    create_sound("oops.wav")
}

fn main() {
    let s = make_sprite();
}
