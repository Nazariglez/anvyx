// @mode: check
// @expect: error
// @contains: Mismatched types

extern fn native_apply(value: int, cb: fn(int) -> int) -> int;

fn add(a: int, b: int) -> int { a + b }

fn main() {
    let result = native_apply(5, add);
}
