// @mode: check
// @expect: error
// @contains: Mismatched types

extern type Point {
    fn new(x: float, y: float) -> Self;
}

extern type Rect {
    fn new(x: float, y: float, w: float, h: float) -> Self;
    fn contains(self, p: Point) -> bool;
}

fn main() {
    let r = Rect.new(0.0, 0.0, 10.0, 10.0);
    let r2 = Rect.new(1.0, 1.0, 5.0, 5.0);
    r.contains(r2);
}
