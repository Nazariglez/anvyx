// @mode: check
// @expect: success

extend<K, V> [K: V] {
    fn size(var self, k: K, v: V) {
        self.insert(k, v);
    }
}

fn main() {
    var m: [string: int] = ["a": 1];
    m.size("b", 2);
}
