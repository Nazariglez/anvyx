// @mode: check
// @expect: error
// @contains: already exists

struct Pair<T> {
    first: T,
    second: T,

    fn get_first(self) -> T { self.first }
}

extend Pair<T> {
    fn get_first(self) -> T { self.first }
}

fn main() {}
