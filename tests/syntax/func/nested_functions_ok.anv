// @mode: check
// @expect: success

fn main() {
    fn add(a: int, b: int) -> int { a + b }
    let c: int = add(10, 15);

    fn twice(a: int) -> int {
        return add(a, a);
    }

    let d: int = twice(5);
}
