// @mode: check
// @expect: success

fn first<T, N: int>(arr: [T; N]) -> T {
    arr[0]
}

fn main() {
    let x = first([1, 2, 3]);
    let _: int = x;

    let y = first(["a", "b"]);
    let _: string = y;
}
