// @mode: check
// @expect: success

fn head(xs: [int; ..]) -> int {
    xs[0]
}

fn get_second(xs: [int; ..]) -> int {
    xs[1]
}

fn main() {
    let arr = [1, 2, 3];
    let _ = head(arr);
    let arr2 = [10, 20, 30];
    let _ = get_second(arr2);
}

