// @mode: check
// @expect: success
// @skip: name of test is wrong, and also, I am not sure this should be equal? 

fn foo() -> int { 0 }
fn bar() -> int { 1 }

fn main() {
    let a: fn() -> int = foo;
    let b: fn() -> int = bar;
    let eq = a == b;
}
